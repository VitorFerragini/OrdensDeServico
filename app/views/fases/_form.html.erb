<%= form_with(model: fase, local: true) do |form| %>
  <% if fase.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(fase.errors.count, "error") %> prohibited this fase from being saved:</h2>

      <ul>
        <% fase.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nome %>
    <%= form.text_field :nome %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

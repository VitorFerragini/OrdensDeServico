<%= form_with(model: pa, local: true) do |form| %>
  <% if pa.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pa.errors.count, "error") %> prohibited this pa from being saved:</h2>

      <ul>
        <% pa.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :numero %>
    <%= form.text_field :numero %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
